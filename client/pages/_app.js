import 'styles/globals.css';  // Assurez-vous d'avoir ce fichier CSS ou ajustez le chemin selon vos besoins

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
