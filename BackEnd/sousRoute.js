// articles.js
const express = require('express');
const router = express.Router();

router.get('/', (req, res) => {
  // Your logic for handling /articles route
});

router.post('/', (req, res) => {
  // Your logic for handling POST requests to /articles
});

// Export the router
module.exports = router;
